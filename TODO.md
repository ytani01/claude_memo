# TODO

**残っている項目: 2 件。**
新しく足すときは「完了済み」の上に節を作る。**番号は `TODO-001` から。**

---

## TODO-001. スマホ縦画面で 16:9 のまま幅いっぱいに表示する

PC ブラウザ（16:9）向けには調整済みだが、スマホの縦画面が未調整。
既に `@media screen and (max-width: 640px)` で `.video-viewport` を
`aspect-ratio: auto` にしている節（132〜160 行あたり）があり、
今回決めた方針とは逆になっている。

**方針**（利用者と決定済み）: 16:9 の枠は保ったまま、画面幅いっぱいに縮める。
コントロール類とプレイリストは枠の下に縦に並べる。
スライドの中身（`render()`）は触らず、`cqw` / `clamp()` の拡大縮小に任せる。

- 対象: `claude_memo.html` の CSS（`@media` の節）とプレイヤー周りのレイアウト
- 完了条件: スマホ縦（360〜430px 幅）で 16:9 の枠が横幅いっぱいに収まり、
  文字が枠からはみ出さない。PC の 16:9 表示が今までどおりであること
- 検証: ブラウザの端末エミュレーションで縦横それぞれを確認

| | 分担 | モデル |
|---|---|---|
| 見込み | 設計・実装 = implementer / 確認 = verifier / レビュー = reviewer | Opus / Sonnet / Opus |

## TODO-002. Android Chrome での読み上げの挙動を調べる

スマホ（Android, Chrome）での音声再生が未検証。**調査のみの項目**で、
直しは結果を見てから別項目にする。

- 調べること:
  - `speech`（Web Speech API）— 自動再生制限、`speechSynthesis` の中断、
    日本語音声の有無
  - `online`（Google Translate TTS を `Audio` で再生）— 自動再生制限、
    180 文字での切り詰め
- 実機での確認は利用者にお願いする
- 完了条件: 2 系統それぞれについて、Android Chrome で何が起きるかと
  直すべき点が整理されていること

| | 分担 | モデル |
|---|---|---|
| 見込み | main のみ（調べて報告するだけ） | Opus |

---

## 完了済み

1 項目 1 ファイル。`archives/todo/` にある（新しい順）。
**やらないと決めたものの理由もそこにある。** 蒸し返す前に読むこと。

（まだ無し）
