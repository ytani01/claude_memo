# TODO

**残っている項目: TODO-005。** これまでに 4 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。**番号は `TODO-006` から。**

---

## TODO-005. スライドのタップで再生と一時停止を切り替える

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5 / effort high | verifier + reviewer |

- [ ] スライドのクリック / タップで `togglePlay()` を呼ぶ
- [ ] 切り替えたとき、中央に ▶ / ‖ のアイコンを一瞬出す
- [ ] スマホのフルスクリーンで、タップが効くことと暗幕で抜けられることを確かめる

フルスクリーン中のスマホは、再生ボタンが暗幕の裏に入って押せない。
物理キーボードも無いので、Space も使えない。
いまタップで効くのは暗幕（フルスクリーンを抜ける）だけ。

スライドをタップしたら再生と一時停止が切り替わるようにする。**通常表示でも
効かせる**（動画プレイヤーと同じ振る舞いで、条件分岐が要らない）。
反応が分かるよう、中央に ▶ / ‖ を 0.5 秒ほど出して消す。

- 対象は `#player-viewport`（または `#slide-canvas`）の click。
  暗幕のハンドラは `#viewport-stage` の click で `e.target === stage` の
  ときだけ反応するので、干渉しない
- keydown の Space と同じく、`togglePlay()` の前に
  `speechSynthesis.resume()` を呼ぶ
- スマホでは中身が `--vp-scale` で縮小されるので、アイコンも
  `cqw` か `clamp()` で置く（`px` 直書きは崩れる）

## 完了済み

1 項目 1 ファイル。`archives/todo/` にある（新しい順）。
**やらないと決めたものの理由もそこにある。** 蒸し返す前に読むこと。

- [**TODO-004.** 横持ちのスマホでフルスクリーンの高さを画面に合わせる](archives/todo/TODO-004.%20横持ちのスマホでフルスクリーンの高さを画面に合わせる.md)
- [**TODO-003.** 横持ちのスマホでフルスクリーンから抜けられないのを直す](archives/todo/TODO-003.%20横持ちのスマホでフルスクリーンから抜けられないのを直す.md)
- [**TODO-002.** Android Chrome での読み上げを直す](archives/todo/TODO-002.%20Android%20Chrome%20での読み上げを直す.md)
- [**TODO-001.** スマホ縦画面で 16:9 のまま幅いっぱいに表示する](archives/todo/TODO-001.%20スマホ縦画面で%2016:9%20のまま幅いっぱいに表示する.md)
