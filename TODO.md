# TODO

**残っている項目: TODO-002、TODO-003。** これまでに 1 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。**番号は `TODO-004` から。**

---

## TODO-002. Android Chrome での読み上げを直す

スマホ（Android, Chrome）での音声再生が未検証だった。調査の結果、
2 系統とも別々の原因で壊れていることが分かったので、
**この項目で直すところまでやる**（2026-09-18 に利用者と決めた）。

調査メモは `archives/agents/TODO-002/main-investigation.md`。

**分かっている原因**:
- **Online TTS が鳴らない** — 自動再生のブロック。再生ボタンのハンドラ
  （`claude_memo.html:1546`）は Web Speech API だけを unlock しており、
  `Audio` 要素は unlock していない。`speakOnlineTTS()` は毎回
  `new Audio(url)` を作り、`setTimeout` や読み終わりイベントの中から
  `play()` するので Android Chrome では reject される。
  直し方: 再生ボタンを押した瞬間に `Audio` を 1 つ unlock し、
  以後は `src` の差し替えで使い回す
- **Web Speech が途中で途切れる** — `chromeResumeTimer`
  （`claude_memo.html:1322`）の 5 秒ごとの `pause()` / `resume()`。
  デスクトップ Chrome 向けの回避策が Android Chrome では戻ってこない。
  直し方: Android では pause/resume を呼ばない

**潰した候補**（蒸し返さないこと）:
- 180 文字の切り詰め — 読み変換後で最長 164 文字。切り捨ては 0 枚
- safety timeout の早発火 — 21〜29 秒に対し読み上げは 13〜19 秒
- `duration` で先に進む — 再生ループは頭打ちにするだけ
- Google 側のブロック — 同じ URL が HTTP 200 / `audio/mpeg` を返す

- **実機での確認は利用者にお願いする。** どちらの原因もエミュレーションでは
  再現しないので、直したものを実機で見てもらう必要がある
- 完了条件: Android Chrome で、Web Speech が最後まで読み切り、
  Online TTS も鳴ること。PC での読み上げが今までどおりであること
- **着手は TODO-001 を決着させてから**（同じファイルを同時に触ると競合する）

| | 分担 | モデル |
|---|---|---|
| 見込み | 調査 = main（済） / 実装 = implementer / 確認 = verifier / レビュー = reviewer | Opus / Opus / Sonnet / Opus |

## TODO-003. 横持ち・低い画面でのフルスクリーンの穴をふさぐ

TODO-001 のレビューで見つかった、幅 768px 以上に残る穴。
TODO-001 は縦画面の項目なので、こちらに分けた（2026-09-18 に利用者と決めた）。
根拠は `archives/agents/TODO-001/reviewer-report-3.md` の検討 3・4。

- **スマホを横にするとフルスクリーンから抜けられない。** 844x390 は幅が
  768 以上なので PC 扱いになり、暗幕（`::before`）も `body.fs-lock` も
  効かない。フルスクリーンのボタンは箱の裏で押せず、物理キーボードが
  無ければ再読み込みしか出口が無い。縦に戻せば抜けられる
- **幅 767px で高さが 650px を切ると、フルスクリーン中の字幕が画面外に出て
  切られる。** 字幕は `top: 100%` + `margin-top: 0.75rem` で枠の下に置くので、
  下の暗幕の帯に 109.5px 以上が要る（条件は `高さ < 幅 * 9/16 + 219`）。
  767x600 で 25.2px、767x500 で 97.5px 中 75.2px がはみ出す
- **横持ちでは枠の高さが画面の高さを超える**（844x390 で枠は 448px）ので、
  スライド全体を見るには縦スクロールが要る。これは変更前から同じ

**決めること**: 幅で「スマホかどうか」を判定するのをやめ、
`(pointer: coarse)` を併用するか。高さも見て縮小するか。

| | 分担 | モデル |
|---|---|---|
| 見込み | 設計・実装 = implementer / 確認 = verifier / レビュー = reviewer | Opus / Sonnet / Opus |

---

## 完了済み

1 項目 1 ファイル。`archives/todo/` にある（新しい順）。
**やらないと決めたものの理由もそこにある。** 蒸し返す前に読むこと。

- [**TODO-001.** スマホ縦画面で 16:9 のまま幅いっぱいに表示する](archives/todo/TODO-001.%20スマホ縦画面で%2016:9%20のまま幅いっぱいに表示する.md)
