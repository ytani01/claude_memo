# TODO-007. スライド 12 の TODO.md 記述例から `/clear` の行を外す

（ファイル名には `/` を使えないので、見出しの `/clear` を `clear` にしてある）

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5 / effort high | main のみ |
| 実施 | Opus 5 / effort high | main のみ |

| 担当 | モデル | effort | output | cache_creation | 料金の割合 |
|------|--------|--------|--------|----------------|-----------|
| main | Opus 5 | high | 1,610 | 4,861 | 100% |
| 合計 |  |  | 1,610 | 4,861 | 概算 $0.2 |

## きっかけ

スライド 12「TODO.md の実際の記述例」のチェックリストに
「Git コミット作成後に `/clear` を実行してセッションリセット」という行が
あった。実際の `TODO.md` に `/clear` の実行は書いていない。`/clear` を
勧めるのは `~/.claude/CLAUDE.md` と `todo-close` skill の側の話で、
項目のチェックリストに書く運用はしていない。「実際の記述例」と題した
スライドに実物に無い行が載っているのは誤り。

## やったこと

`claude_memo.html` の 2 箇所（スライド 12）。

- チェックリストの `/clear` の行を、決着時のコミットの行に差し替えた
  （`feat(core): クラス構成の刷新（TODO-056）`）。コードの部分が他の行より
  長くならないよう文面を詰めた
- narration の「ツールによる検証結果やコミット後のクリア手順まで明記します」を
  「ツールによる検証結果や決着時のコミットまで明記します」に直した

## 確かめたこと

- `git diff --stat` は `claude_memo.html` の 2 行のみ（2 insertions, 2 deletions）
- 差し替えた行の HTML 構造（`div` / `span` / `code` のクラス）は元のまま
- 読み上げの置換（`/clear` → 「スラッシュ クリア」）は他のスライドの
  narration 向けで、今回の変更で影響を受けるものは無い
