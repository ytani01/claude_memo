# TODO-044 の分担

`docs/Developer.md` に節を 1 つ足す項目。本文は main が書き、
**手順の再現と主張の裏取りを verifier に分けた。**

文書だけの項目だが、`python3 -m http.server` で試せる手順と、
**「ローカルを指す参照は 1 か所だけ」という数え上げ**が入る。後者は
1 つでも見落とせば文書の主張が崩れるので、別の目で数えさせた。

挙動を変える項目ではないので、レビューの担当は立てていない。

| 担当 | モデル | 依頼した内容 | 報告 |
|------|--------|--------------|------|
| verifier | Sonnet 5 / effort medium（定義のまま） | 別ディレクトリへコピーして `http.server` で 200 を再現、ローカル参照の洗い出し、CDN の列挙と `claude_memo.html` の照合 | [verifier-report.md](verifier-report.md) |

食い違いは 0 件だった。`file://` は検証できない環境なので、文書の
「試していない」という書き方が妥当かだけ見させた。
