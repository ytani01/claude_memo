# reviewer への依頼（TODO-016）

## 目的

差分を見て、**描画結果と動作が変わっていないか**を判定する。
この項目の合否はそこにある。**コードは直さない。** 報告だけする。

## 読むもの

- 依頼: `archives/agents/TODO-016/implementer-request.md`
- 実装の報告: `archives/agents/TODO-016/implementer-report.md`
- 差分: `git diff claude_memo.html`（未コミット）
- 参考: `archives/todo/TODO-014. 見た目と動作を変えない範囲でコードの重複を整理する.md`
  （同じ趣旨の前回。何をどこまで見たか）

## 見ること

- **CSS**: `width: 100%` / `max-width: 100%` の削除と `@media` の統合で、
  計算値と優先順位が変わらないか。特に
  - `#viewport-frame` に JS（`setupViewportScale`）が `max-width` を
    インラインで入れる経路との噛み合わせ
  - 統合後の `@media` の中で、規則の**順序**が変わったことによる上書きの差
  - フルスクリーン時（`.pseudo-fullscreen` / `.is-fullscreen`）の挙動
- **JS**: 短くした 3 ハンドラ（字幕トグル・音声エンジン切替・速度の巡回）と
  `slideStartTimes` の生成が、元と**同じ値・同じクラス集合**になるか。
  境界（速度が一周する所、`indexOf` が -1 になる所）を具体的に確かめる
- `speechActive` の削除で落ちた副作用が無いか
- `CLAUDE.md`（利用者全体・プロジェクト）の規約に照らして問題が無いか
- コメントが実態と合っているか

## 完了条件

「動作が変わるもの」が 0 件か、あるなら**どの操作で、どう変わるか**が
報告にある。疑わしいものは実際に動かして確かめ、確かめられなければ
「確かめられなかった」と書く。

## 報告

`archives/agents/TODO-016/reviewer-report.md` に書く。
中身は **動作が変わるもの / 規約との食い違い / 気づき** に絞る。
返事は「終わったか・報告ファイルのパス・判断が要る点」を 5 行以内で。

目安 25 分。超えそうなら、そこまでの状態を報告する。
