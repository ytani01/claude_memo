# TODO-023 追加確認: prepareSpeechText 置換 3 件の影響

## 1. 3 パターンが当たる箇所（全 17 枚を機械的に洗い出し）

`claude_memo.html` の全 `slideData` から `id` と `narration` を正規表現で抽出し
（17 件抽出できたことを確認 = 全スライド分）、3 パターンを検索した結果:

```
total slides found: 17
2 ['考え方']
10 ['main', 'implementer']
```

- スライド 2（`考え方`）、スライド 10（`main`, `implementer`）の 2 枚のみ。
  他の 15 枚には該当無し。
- `\bmain\b` の語境界チェック: 全 narration 中で `main` を大文字小文字問わず
  含む単語を正規表現 `[A-Za-z]+` で抜き出したところ、ヒットしたのは
  スライド 10 の `main` という単語 1 個だけだった。`domain` や `maintain` の
  ような語を誤って拾っている箇所は無い。

→ main の把握（スライド 2 = 考え方、スライド 10 = main と implementer 各 1 か所）
  と一致。他に当たる箇所は無い。

## 2. 差分の範囲

```
git diff claude_memo.html | grep "^@@"
@@ -512,34 +512,37 @@
@@ -1252,7 +1255,10 @@
```

2 個目のハンク（`prepareSpeechText()` 内）:

```diff
-                .replace(/auto-mode/gi, 'オートモード');
+                .replace(/auto-mode/gi, 'オートモード')
+                .replace(/考え方/g, 'かんがえかた')
+                .replace(/\bmain\b/gi, 'メイン')
+                .replace(/\bimplementer\b/gi, 'インプリメンター');
```

3 行追加のみ。1 個目のハンク（スライド 2 の本文）は前回の verifier-report.md で
確認済みの内容と同一（今回の追加確認の対象ではない）。
→ **依頼どおり、差分は `prepareSpeechText()` への 3 行追加だけに収まっている。**

## 3. 秒数の実測

前回と同じ手順（`prepareSpeechText()` 相当の置換を Python で再現 →
`.substring(0,180)` → Google Translate TTS を `curl --referer '' -A 'Mozilla/5.0'`
で取得 → `ffprobe` で秒数）。

### スライド 2（`duration: 19`）

置換後テキスト（127 文字、180 未満なので全文）:
`このあとの流れです。まず常時稼働させている利用環境、次に日常の対話のしかたと設定のかんがえかた、そして最も重要な トゥードゥー ドット エムディー によるタスク管理、さらにマルチエージェントでの役割分担、最後に現状の課題とまとめ、という順にご紹介します。`

```
ffprobe slide2_v2.mp3 → 26.304000
26.304 / 1.4 = 18.788... → 四捨五入 19
```

→ `duration: 19` と**一致**（前回計測の 26.256 秒とほぼ同じで、`考え方` の
置換追加による影響は無視できる差）。

### スライド 10（`duration: 20`）

置換後テキスト（145 文字、180 未満なので全文）:
`マルチエージェントで役割分担を行っています。全体指揮とレビューには知能の高い Opus を、実装や検証には高速な Sonnet を、日本語校正には Haiku を割り当てています。直接ファイルを編集できるのは メイン と インプリメンター だけに制限し、コストと精度の最適化を図っています。`

```
ffprobe slide10_v2.mp3  → 27.216000
ffprobe slide10_v2b.mp3 → 27.216000  （再取得して再現性を確認、同一値）
27.216 / 1.4 = 19.44 → 四捨五入 19
```

→ **`duration: 20` と一致しない。** 実測に基づく正しい値は **19**。

## 結論

| スライド | 現在の duration | 実測（1.4 で割った四捨五入） | 判定 |
|---|---|---|---|
| 2 | 19 | 19（26.304s） | 一致 |
| 10 | 20 | **19**（27.216s、再取得でも同値） | **不一致** |

スライド 10 の `duration` を 20 → **19** に直す必要がある（修正は行っていない）。

## 確かめられなかったこと

- スライド 10 の `duration: 20` が、`main`/`implementer` を英語のまま読ませていた
  旧テキストでの実測値なのか、別の理由で決めた値なのかは確認していない
  （旧テキストでの秒数は計測していない）。今回計測したのは現行の置換ルール
  適用後のテキストのみで、それが `duration: 19` を要求している、という点のみ
  確認できる。
- Google Translate TTS の合成結果は将来変動する可能性がある。今回は同一入力で
  2 回取得し同じ 27.216 秒だったため、再現性は確認済み。
