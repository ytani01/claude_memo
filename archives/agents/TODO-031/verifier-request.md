# TODO-031 検証依頼

## 目的

`prepareSpeechText()` に「使い方」→「つかいかた」を足した。
**誤爆していないか**と、**`duration` がずれていないか**を確かめる。

## 対象

- `claude_memo.html` の `prepareSpeechText()`
- `tools/measure-duration.py` の `RULES`（写し）

どちらも `考え方` の行の直後に 1 行足しただけ（`git diff HEAD`）。

## 完了条件

**1. 2 か所が同じであること。** `claude_memo.html` の
`.replace(/使い方/g, 'つかいかた')` と、`tools/measure-duration.py` の
`(r'使い方', 'つかいかた', 0)` が、**パターン・置換後・大文字小文字の
扱い・置換表の中での位置**まで揃っていること。
（写し元がずれると測った秒数が実際とずれる。TODO-030 参照）

**2. 誤爆していないこと。** `使い方` は部分一致で拾う。
**17 枚すべての `narration` に `prepareSpeechText()` を掛けた結果を
目で見て**、意図しない置換が起きていないか確かめる。
とくに `使い方法` のように後ろに字が続く語が無いか。
（`tools/measure-duration.py --all` の「読み ◯字」でも変化は分かるが、
**文字列そのものを出して見ること**）

**3. `duration` がずれていないこと。**
`使い方` が narration に入っているのはスライド 1・2・17 のはず。
**それが本当に 3 枚だけか**を確かめたうえで、
`tools/measure-duration.py --all` で 17 枚を測り、`slideData` の
`duration` と突き合わせる。

- main の実測では 1 = 16、2 = 19、17 = 23 で、**3 枚とも変わっていない**
- **スライド 15 は実測 16 に対し実装 17 でずれているが、これは既知**
  （TODO-032 として別に立てた）。**この項目では直さない**
- それ以外にずれている枚があれば、枚数と値を報告する

**4. スライド 17 が 180 字を超えていないこと。**
読み上げ用の文字列が 174 → 176 字になったはず。180 を超えると
Online TTS で末尾が切れる。**残りの余裕を数字で報告すること。**

## やらないこと

**コードは直さない。** レイアウトの測り直しは要らない（画面は変えていない）。

## 報告

`archives/agents/TODO-031/verifier-report.md` に書く。
**返事は 5 行以内**。誤爆の確認は「見た」で済ませず、**何枚分の
読み上げ文字列を目で見たか**を数字で書くこと。

## 目安

10〜15 分。
