# TODO-031 検証報告

## 結論

**問題無し。** 完了条件はすべて満たしている（スライド 15 の既知のずれ
（TODO-032）を除く）。コードは直していない。

## 変更ファイルの範囲

`git diff HEAD` で確認。`claude_memo.html` の `prepareSpeechText()` と
`tools/measure-duration.py` の `RULES` に、それぞれ `考え方` の行の直後に
1 行足しただけ。他の変更は無い。

## 1. 2 か所の突き合わせ

```
claude_memo.html:
    .replace(/使い方/g, 'つかいかた')

tools/measure-duration.py:
    (r'使い方', 'つかいかた', 0),
```

- パターン: `使い方` / `使い方` → 一致
- 置換後: `つかいかた` / `つかいかた` → 一致
- 大文字小文字の扱い: JS は `g`（`i` 無し＝大小区別あり）、Python は
  フラグ `0`（`re.I` 無し＝大小区別あり）→ 一致
- 置換表での位置: どちらも `考え方` の直後、`\bmain\b` の直前 → 一致

2 か所は完全に揃っている。

## 2. 誤爆の確認

17 枚すべての `narration` に、`RULES` の置換表を Python で再現して掛け、
**17 枚分の置換後の文字列を全部出力して目視で確認した**。

`使い方` を含んでいたのは **3 枚**（スライド 1・2・17）で、依頼書の
想定どおり。それぞれ次のとおり置換されている。

- スライド 1: 「クロード コード の**つかいかた**と工夫を」
- スライド 2: 「その他の便利な**つかいかた**」
- スライド 17: 「便利な**つかいかた**もご紹介しました」

残り 14 枚（スライド 3〜16 のうち 1・2・17 を除く）には `使い方` を
含む語も、`使い方法` のように後ろに字が続いて意図せず巻き込まれる語も
無かった（全文を目視、該当なし）。

**誤爆は無い。17 枚全部を目視で確認した。**

## 3. `duration` の突き合わせ

`tools/measure-duration.py --all` の実測と、
`grep -oP "duration:\s*\K[0-9]+" claude_memo.html` で拾った実装値を
17 枚分突き合わせた。

| slide | 実測 | 実装 | 判定 |
|---|---|---|---|
| 1 | 16 | 16 | 一致 |
| 2 | 19 | 19 | 一致 |
| 3 | 19 | 19 | 一致 |
| 4 | 19 | 19 | 一致 |
| 5 | 21 | 21 | 一致 |
| 6 | 19 | 19 | 一致 |
| 7 | 19 | 19 | 一致 |
| 8 | 19 | 19 | 一致 |
| 9 | 18 | 18 | 一致 |
| 10 | 21 | 21 | 一致 |
| 11 | 19 | 19 | 一致 |
| 12 | 15 | 15 | 一致 |
| 13 | 17 | 17 | 一致 |
| 14 | 17 | 17 | 一致 |
| 15 | 16 | 17 | **既知のずれ（TODO-032）。今回は対応しない** |
| 16 | 18 | 18 | 一致 |
| 17 | 23 | 23 | 一致 |

- スライド 1・2・17 は main の実測どおり **16 / 19 / 23 のまま変化無し**
- スライド 15 以外の 16 枚はすべて一致
- スライド 15 は依頼書のとおり既知のずれ（TODO-032）で、この項目では
  直していない

## 4. スライド 17 の文字数

```
スライド 17: 原文 162 字 / 読み 176 字 / 実測 32.712s / 1.4 倍速 23.37s -> duration: 23
```

読み上げ用の文字列は **176 字**（174 → 176 字になった想定どおり）。
`TTS_MAX_CHARS = 180` に対して **余裕 4 字**。180 字は超えていない。

## やらなかったこと・判断できないこと

特になし。レイアウトの測り直しは依頼どおり行っていない（画面は
変えていないため）。
