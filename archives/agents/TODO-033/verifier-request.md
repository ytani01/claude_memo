# TODO-033 verifier への依頼

## 目的

スライドの並べ替えと、スライド 1・2 の書き換えが、指示どおりで壊れていないかを
確かめる。**コードは直さない。** 見つけたことは報告だけする。

## 対象

`claude_memo.html`（`slideData` と各 `render()`）、`CLAUDE.md` の合計秒数。
差分は `git diff` で見られる（未コミット）。

## 変更の内容

1. スライドを並べ替えた（旧番号 → 新番号）。`TODO.md` の TODO-033 の表を見ること
2. スライド 2（全体の概要）の目次 6 項目とナレーションを新しい順番に合わせた
3. スライド 1 の本文を副題 1 行「毎日の開発を、どう任せているか。」に差し替え、
   ナレーションを挨拶 + 予防線から入る形に書き直した
4. `duration` を再測定（スライド 1: 16 → 20、スライド 2: 19 → 21）。
   合計 315 → 321 秒。`CLAUDE.md` の記載も直した

## 確かめること

- [ ] 17 枚の `id` が 1〜17 の連番で、`// Slide N` のコメントと一致している
- [ ] 並び順が `TODO.md` の TODO-033 の表のとおり
- [ ] スライド 1・2 の `duration` が `tools/measure-duration.py 1 2` の実測と一致
- [ ] `duration` の合計が 321 秒で、`CLAUDE.md` の記載および
      画面の合計時間表示（`formatTime(321)` = `5:21`）と一致
- [ ] スライド 2 の目次の 6 項目が、実際のスライドの並びと食い違っていない
- [ ] 前後のスライドを指す言い回し（「このあと」「先ほど」の類い）が、
      並べ替え後の順番と矛盾していないか、全 17 枚のナレーションを読んで確認
- [ ] Playwright でページを開き、コンソールのエラー 0 件、17 枚への送りと再生が動く
- [ ] ページ全体のはみ出しと `#slide-canvas` の内側の見切れが、
      変更前（`git stash` で戻せる）と比べて増えていないこと。
      測る条件は PC 通常表示・PC フルスクリーン・横持ちスマホ 844x390 の
      通常表示とフルスクリーンの 4 つ（TODO-027 と同じ）

## 報告

`archives/agents/TODO-033/verifier-report.md` に書く。
返事は 5 行以内（終わったか・報告ファイルのパス・判断が要る点）。
目安の所要は 15 分程度。長引くなら途中でも報告すること。
