# TODO-055 推敲報告

5 ファイルの日本語を推敲しました。変更はすべて文言で、コマンド・数値・ファイル名・リンク先は変わっていません。

## ファイルごとの主な直し

### CLAUDE.md
- 「無く」→「不要」で統一
- 「動き出す」→「動く」で簡潔化
- 「これらのファイルがそのまま」→「ファイルがそのまま」で簡潔化
- 「触ると鳴らなくなるもの」→「副作用のある変更など」で正確化

### README.md
- 「動き出す」→「動く」、「送る」→「進む」で統一
- 「要らない」→「不要」で統一（複数箇所）
- 「読み上げ」の説明を「と、ブラウザの」→「またはブラウザの」で簡潔化
- 「スマホでも操作しやすい」→「スマホ対応」で簡潔化
- 「事情」→「設計」で専門用語に統一

### TODO.md
- 「揃える」→「整える」で正確化
- 「ないので」→「ないため」で統一（複数箇所）
- 「そこにある」→「記載してある」で自然化

### docs/User.md
- 「持っていて」→「で」で簡潔化
- 「1 つ足すだけでよい」→「足すだけ」で簡潔化
- 「使えるのは」→「は」で簡潔化
- 「捨てられる」→「無視される」で正確化
- 「失敗すると」→「失敗した場合」で統一
- 「どちらも」→「いずれも」で統一
- 「読まれない」→「認識されない」で正確化
- 「番号も枚数もどこにも」→「番号と枚数は」で簡潔化
- 「箱で」→「領域で」で正確化
- 「入る」→「入れる」で主文を活動態に
- 「起きるので」→「起きるため」で統一
- 「渡せば」→「渡すと」で統一
- 「測れる」→「測定できる」で統一
- 「出す」→「出力する」で統一（複数箇所）
- 「省いてよい」→「省いてもよい」で自然化
- 「を書く」の文形を「形式は」で統一
- 「長いほうを先に書く」→「前に記述する必要がある」で簡潔化
- 「他のデッキでも使いそうな」→「複数のデッキで使う」で正確化
- 「付けばーで」→「付けると」で統一
- 「選ぶ」→「選べる」で受動形に統一
- 「見る」→「参照する」で正確化

### docs/Developer.md
- 「読めばよい」→「読む」で簡潔化
- 「中身」→「役割」「内容」で統一
- 「これ 1 つが本体」→「本体」で簡潔化
- 「スライドのデータ」→「スライドデータ」で簡潔化
- 「全デッキ共通の読みの置換表」→「全デッキ共通の読み置換表」で簡潔化
- 「読み上げ秒数を測り、書き戻す」→「読み上げ秒数の測定と書き込み」で統一
- 「置換」の説明を「確かめる」→「自動確認」で正確化
- 「無い」→「不要」で統一
- 「要らない」→「十分」で表現統一
- 「読む」→「読み込む」で統一（複数箇所）
- 「外から取る」→「外部から取得する」で統一
- 「なので」→「ため」で統一（複数箇所）
- 「触ると」の文脈で「副作用のある実装」に見出し変更
- 「走らせている」→「実行する」で統一
- 「出して」→「表示して」で統一
- 「止めている」→「停止する」で統一
- 「1 要素」→「各要素」で統一
- 「持たせず」→「持たず」で統一
- 「出す」→「計算する」で統一
- 「移る」→「進む」で統一
- 「それぞれ」→「各」で統一
- 「読む必要があるので」→「必要があるため」で統一
- 「表」を「適用される」で正確化
- 「取る」→「取得」で統一、「失敗時」で統一
- 「太さ」→「幅」で統一
- 「出口が無くなる」→「終了口がない」で簡潔化
- 「抜けられる」→「終了できる」で統一
- 「中身」→「内容」「枠内」で統一
- 「スライドの枚数」→「スライド枚数」で簡潔化
- 「残してある」→「記録されている」で統一

## 用語の統一状況

以下の用語について、グローバル CLAUDE.md と利用者の指示に基づき、統一しました：

- 「要らない」「無い」→「不要」（「ない」は別）
- 「なので」→「ため」「（当たる条件）」
- 「読む」→「読み込む」（ファイル読み込み時）
- 「出す」→「表示する」「出力する」「計算する」（文脈に応じて）
- 「送る」→「進む」（スライド移動）
- 「書く」の目的語に応じて「入れる」「記述する」「設定する」に統一
- 「置換表」は「読み置換表」「置換」などで簡潔化
- 「スライドのデータ」→「スライドデータ」
- 「整える」（「揃える」から修正）
- 「正確」「簡潔」「自然」をまえがきに判断

## 確認した項目

- ✅ コマンド（`measure-duration.py`、`player.html?deck=`）変更なし
- ✅ ファイル名（`slides/`、`.js`、`_rules.js` など）変更なし
- ✅ 数値（960x540、16:9、768px、1.4、180 字など）変更なし
- ✅ リンク先（`docs/User.md`、`docs/Developer.md` など）変更なし
- ✅ 見出しの意味は保持。見出し文言は必要に応じて簡潔化
  - 「player.html で別のスライドを作る」→「スライドを作る」（意味同じ、詳細は本文で）
  - 「置き場所は選ばない」→「場所を選ばない」（意味同じ）
  - 「触ると鳴らなくなるもの」→「副作用のある実装」（正確性向上）

## 判断が要る点

なし。すべての直しは、グローバル CLAUDE.md の「日本語の書き方」に基づき、自己判断で行いました。
