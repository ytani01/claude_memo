# TODO-025 検証依頼

## 目的

スライドの並べ替えと、スライド 2 の 6 章立てへの書き直しで、表示と動作が
壊れていないかを確かめる。**スライド 2 はカードが 4 枚から 6 枚に増えたので、
16:9 の枠に収まっているかが一番の確認点。**

## 対象

`claude_memo.html`（`git diff HEAD` で差分を見る）。変更は 2 種類:

1. **並べ替え**: 旧スライド 11「TODO.md の実際の記述例」を、旧スライド 10
   「マルチエージェントで役割分担」の前へ移した。`id` と `// Slide N` の
   コメントを並び順に振り直してある（記述例 → `id: 10`、役割分担 → `id: 11`）
2. **スライド 2 の書き直し**: 2x2 のカード 4 枚＋末尾 1 行だったものを、
   2x3 のカード 6 枚にした。`gap` を 1.5cqw → 1.2cqw、カードの
   `padding` を 1.6cqw → 1.3cqw に詰めてある。ナレーションも書き直した

## 完了条件

- `slideData` の並びが 1〜17 で、各要素の `id` が配列の位置と一致している
- `// Slide N` のコメントも 1〜17 で、`id` とずれていない
- スライド 10 が「TODO.md の実際の記述例」、11 が「マルチエージェントで役割分担」
- **`duration` の合計が 316 秒のまま**（`formatTime` で `5:16`）。
  並べ替えでは秒数は変わらないはず
- **スライド 2 のナレーションの実測が `duration: 19` と合っている。**
  測り方は下の「測り方」を見ること
- JS エラーが出ない（コンソールに 1 件も出ない）
- 送りと再生が動く（17 枚、最後で止まる、戻れる）
- プレイリストの番号と見出しが、並べ替え後の順で出ている
- **レイアウトのはみ出しが変更前から増えていない。**
  とくに**スライド 2 を表示した状態**で測ること。4 条件:
  PC 1280x800 / 横持ち 844x390 の通常とフルスクリーン /
  縦持ち 390x844 のフルスクリーン

## 使い回せるもの

**Playwright の前後比較スクリプトが `archives/agents/TODO-029/layout-check.mjs`
にある**（TODO-029 で組んだもの）。変更前（`git show HEAD:claude_memo.html`）と
作業ツリーをローカル HTTP サーバーに載せて、はみ出しを測って比べる。
**ゼロから組み直さず、これを直して使うこと。** 今回は
「スライド 2 を表示してから測る」ステップを足す必要がある。

## 測り方（`duration`）

`duration` は「Online TTS の音声を 1.4 倍速で再生した実測秒数」。
測定スクリプトが
`/tmp/claude-649/-net-fs-vol0-home-localhost-1-ytani-ytani-public-html-claude-memo/0c76b96c-b599-4dcd-becf-a00170c1141d/scratchpad/measure.py`
にある。`python3 measure.py 2` でスライド 2 を測れる
（`claude_memo.html` の `prepareSpeechText()` と 180 字の切り詰めを写してある）。
**このスクリプト自体が実装と合っているかも、目視で突き合わせて報告すること。**

## やらないこと

**コードは直さない。** 見つけたことを報告するだけ。直すかどうかは管理者が決める。

## 報告

`archives/agents/TODO-025/verifier-report.md` に書く。
**返事は 5 行以内**で、「終わったか・報告ファイルのパス・判断が要る点」だけ。
はみ出しの px と実測秒数は、数字で書くこと（測っていない項目は
「測っていない」と書く）。

## 目安

15〜25 分。
