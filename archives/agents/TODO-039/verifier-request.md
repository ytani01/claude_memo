# TODO-039 verifier への依頼

## 目的
スライド 6（マルチエージェントで役割分担）に注釈を 1 行足し、ナレーションに
一言足した。表示がはみ出さないこと、`duration` が実測と合うことを確かめる。

## 対象
- `claude_memo.html` のスライド 6（`id: 6`）。直前のコミットからの差分

## やること
1. `tools/measure-duration.py 6` を実行し、実測秒数と `duration` に入れる値を
   報告する（★180 字で切れる、が出ないことも確認）。現在の `duration` は 19
2. スライド 6 を次の 2 つの画面サイズで表示し、注釈まで含めて枠内に収まって
   いるか（本文が切れていないか、下にはみ出していないか）を確認する
   - 1280 x 720（PC）
   - 844 x 390（横持ちスマホ）
   スクリーンショットは `~/tmp/playwright-mcp/` に内容の分かる名前で保存する

## 完了条件
- 実測秒数と、`duration` に入れるべき値が報告に書かれている
- 2 つの画面サイズそれぞれについて、収まっているかの判定と根拠（画像）がある

## 報告
`archives/agents/TODO-039/verifier-report.md` に書く。返事は 5 行以内で
「終わったか・報告ファイルのパス・判断が要る点」だけ。

コードは直さないこと。見つけたことは報告するだけ。
目安は 10 分程度。ネットワークや playwright が使えないときは、その旨を
報告して終わること（回避策を探して長居しない）。
