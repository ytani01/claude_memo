# TODO-028 検証依頼

## 目的

スライド 16（現状の課題）の 1 つ目の課題を、スライド 5（`/clear`）と
13（複数アカウント）を受ける書き方に直した。表示と秒数が壊れていないかを
確かめる。

## 対象

`claude_memo.html` と `CLAUDE.md`（`git diff HEAD` で差分を見る）。

1. 1 枚目のカードの本文を「長時間の開発において、トークン消費をもっと
   効果的に節約する運用を模索中。」から「`/clear` と複数アカウントで
   凌いでいるが、長時間の開発ではまだ足りない。」に差し替えた
2. ナレーションを書き直し、`duration` を 17 → 18 にした
3. `CLAUDE.md` の合計秒数を 315 → 316 に直した

**カードの枚数も行数も増えていない**（1 文の差し替えだけ）ので、
レイアウトは前回より軽く見てよい。

## 完了条件

- **スライド 16 のナレーションの実測が `duration: 18` と合っている**。
  読み上げ用の文字列が 180 字以内であること（実測 127 字のはず）
- `duration` の合計が 316 秒で、`CLAUDE.md` の記述と一致
  （`formatTime(316)` は `5:16`）
- JS エラーが出ない。送りと再生が動く（17 枚）
- **レイアウトのはみ出しが変更前から増えていない。スライド 16 を表示した
  状態**で 4 条件: PC 1280x800 / 横持ち 844x390 の通常とフルスクリーン /
  縦持ち 390x844 のフルスクリーン
- **`#slide-canvas` の内側で見切れていないこと。** 差し替えた本文が
  元より 2 字短いだけなので、増えていないはずだが確かめる

## 使い回せるもの

**ゼロから組み直さないこと。** `/tmp/verify064/` に残っている
`layout-check-*.mjs` と `internal-overflow-check-*.mjs` の、
表示するスライドを 16 に変えるだけで使える。

## 測り方（`duration`）

`/tmp/claude-649/-net-fs-vol0-home-localhost-1-ytani-ytani-public-html-claude-memo/0c76b96c-b599-4dcd-becf-a00170c1141d/scratchpad/measure.py`
に測定スクリプトがある。`python3 measure.py 16` で測れる。

## やらないこと

**コードは直さない。** 見つけたことを報告するだけ。

## 報告

`archives/agents/TODO-028/verifier-report.md` に新しく書く。
**返事は 5 行以内**。はみ出しの px と実測秒数は数字で書くこと。

## 目安

10〜15 分。
